{% extends 'visiteur/index.html.twig' %}

{% block contenuPage %}
	<h1>Période</h1>
	<fieldset class="mb-3">
		<legend class="col-form-label required">Date</legend>
		<div class="input-group">
			{{form_widget(formFichefrais.mois.month)}}
			<span class="input-group-addon">-</span>
			{{form_widget(formFichefrais.mois.year)}}
		</div>
	</fieldset>
	<br/>

	<table class="table table-bordered table-active">
		<thead class="thead-dark"></thead>
		<tr>
			<th>idVisiteur</th>
			{# <th>mois</th> #}
			<th>nbJustificatifs</th>
			<th>montantValide</th>
			<th>dateModif</th>
			<th>idEtat</th>
		</tr>
		<tbody>
			{% for uneFicheFrais in fichesFrais %}
				<tr>
					<td>{{uneFicheFrais.id}}</td>
					{# <td>{{uneFicheFrais.mois}}</td> #}
					<td>{{uneFicheFrais.nbJustificatifs}}</td>
					<td>{{uneFicheFrais.montantValide}}</td>
					<td>{{uneFicheFrais.dateModif | date('m/Y')}}</td>
					<td>{{uneFicheFrais.idetat.id}}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
{% endblock %}
