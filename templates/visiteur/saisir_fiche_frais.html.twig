{% extends 'visiteur/index.html.twig' %}

{% block contenuPage %}


    {{ dump(fichesFrais) }}

    <div class="example-wrapper">

    <table class="table table-bordered">
        <thead class="thead-dark">
            {# Saisie des fiches de frais #}
        </thead>
        <tr>
            <th>idVisiteur</th>
            <th>mois</th>
            <th>nbJustificatifs</th>
            <th>montantValide</th>
            <th>dateModif</th>
            <th>idEtat</th>
        </tr>
        <tbody>
            {% for uneFicheFrais in fichesFrais %}
            <tr>
                {% for elemFicheFrais in uneFicheFrais %}
                    <td>{{elemFicheFrais}}</td>
                {% endfor %}
            </tr>
            {% endfor %}
        </tbody>
    </table>


{% endblock %}

			{# <tbody>
				{% set nbElemAtelier = (ateliers[0]|length / 2) - 1 %}
				{% for unAtelier in ateliers %}
					<tr>
						{% for i in range(0, nbElemAtelier) %}
							<td>{{unAtelier[i]}}</td>
						{% endfor %}
					</tr>
				{% endfor %}
			</tbody> #}